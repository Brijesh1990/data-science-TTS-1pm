# what is seaborn in python ?
  **defintion** 
  1. A seaborn is a library of python 
  2. A seaborn is used to Matplotlib underneath to plot graphs. 
  
  **how to used or install seaborn**

  ```
   pip --version
   pip install --upgrade pip
   pip install seaborn
  ```